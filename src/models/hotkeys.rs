use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Clone, Debug)]
pub struct Hotkeys {
    pub start: Vec<String>,
    pub pause: Vec<String>,
    pub menu: Vec<String>,
    pub sell: Vec<String>,
    pub upgrade_top_path: Vec<String>,
    pub upgrade_middle_path: Vec<String>,
    pub upgrade_bottom_path: Vec<String>,
    pub dart: Vec<String>,
    pub boomerang: Vec<String>,
    pub bomb: Vec<String>,
    pub tack: Vec<String>,
    pub ice: Vec<String>,
    pub glue: Vec<String>,
    pub sniper: Vec<String>,
    pub sub: Vec<String>,
    pub boat: Vec<String>,
    pub ace: Vec<String>,
    pub heli: Vec<String>,
    pub mortar: Vec<String>,
    pub dartling: Vec<String>,
    pub wizard: Vec<String>,
    pub super_monkey: Vec<String>,
    pub ninja: Vec<String>,
    pub alchemist: Vec<String>,
    pub druid: Vec<String>,
    pub farm: Vec<String>,
    pub spike: Vec<String>,
    pub village: Vec<String>,
    pub engineer: Vec<String>,
    pub beast: Vec<String>,
    pub hero: Vec<String>,
    pub target_priority_right: Vec<String>,
    pub target_priority_left: Vec<String>,
    pub target_priority_special: Vec<String>,
    pub send_next_round: Vec<String>,
    pub road_spikes: Vec<String>,
    pub moab_mine: Vec<String>,
    pub glue_trap: Vec<String>,
    pub camo_trap: Vec<String>,
    pub banana_farmer: Vec<String>,
    pub tech_bot: Vec<String>,
    pub energizing_totem: Vec<String>,
    pub pontoon: Vec<String>,
    pub portable_lake: Vec<String>,
    pub super_monkey_storm: Vec<String>,
    pub monkey_boost: Vec<String>,
    pub thrive: Vec<String>,
    pub time_stop: Vec<String>,
    pub cash_drop: Vec<String>,
    pub copy: Vec<String>,
}
